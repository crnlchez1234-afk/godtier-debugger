llm:
  provider: "transformers"
  model: "TIER10_UPDATE_PACKAGE/Tier10-Specialist-v1"
  timeout: 60

evolution:
  max_generations: 100
  improvement_threshold: 0.05
  auto_approve_threshold: 0.3

paths:
  memory_db: "evolution_memory.db"
  audit_log: "evolution_audit.jsonl"
  snapshots: ".snapshots"

governance:
  strict_mode: true
